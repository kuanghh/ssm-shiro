<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.khh.web.dao.UserMapper">
  <resultMap id="UserResultMap" type="com.khh.web.domain.User">
    <id column="id" jdbcType="VARCHAR" property="id" />
    <result column="name" jdbcType="VARCHAR" property="name" />
    <result column="password" jdbcType="VARCHAR" property="password" />
    <result column="state" jdbcType="INTEGER" property="state" />
    <result column="create_time" jdbcType="TIMESTAMP" property="createTime" />
    <result column="is_valid"  property="isValid" />
  </resultMap>
  <sql id="Base_Column_List">
    id, name, password, state, create_time, is_valid
  </sql>
  <sql id="add_column_list">
    id, name, password, state, create_time, is_valid
  </sql>
  <insert id="insert" parameterType="com.khh.web.domain.User">
    insert into t_user(<include refid="add_column_list" />)
    values(#{id},#{name},#{password},#{state},#{createTime},#{isValid})
  </insert>

  <delete id="deleteById" >
    delete from t_user where id = #{id}
  </delete>

  <update id="update" parameterType="com.khh.web.domain.User" >
    update t_user
    set
      id = id,
      <if test="name != null">
        name = #{name},
      </if>
      <if test="password != null">
        password = #{password},
      </if>
      <if test="state">
        state = #{state},
      </if>
      <if test="createTime != null">
        create_time = #{createTime},
      </if>
      <if test="isValid != null">
          is_valid = #{isValid},
      </if>
      id =id
    where
      id = #{id}
  </update>

  <select id="findById" resultMap="UserResultMap">
      select * from t_user where id=#{id}
  </select>

  <select id="authentication" resultMap="UserResultMap" parameterType="com.khh.web.domain.User">
    select * FROM t_user
    where name=#{name} and password=#{password}
  </select>

  <select id="findByUserName" resultMap="UserResultMap" parameterType="com.khh.web.domain.User">
    select * FROM t_user
    where name=#{name}
  </select>

</mapper>